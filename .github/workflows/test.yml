name: Test_Actions

on: [push]

jobs:
  hello_job:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Greetings
        run: echo "Hello, GitHub Actions!"